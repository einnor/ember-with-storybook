<BsButton
  @type={{type}}
  @outline={{outline}}
  disabled={{disabled}}
  @block={{block}}
  @onClick={{action "onClick"}}
>
  {{#if icon}}
  <FaIcon @icon={{icon}} style="margin-right: 5px;" />
  {{/if}}
  {{yield}}
</BsButton>